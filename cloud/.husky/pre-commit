#!/usr/bin/env sh
# Run lint-staged from the cloud/ package.
# Husky is installed in cloud/ but the repo root uses bun — so we call
# the local pnpm binary which is available in PATH after `pnpm install`.
cd "$(git rev-parse --show-toplevel)/cloud"
pnpm exec lint-staged
